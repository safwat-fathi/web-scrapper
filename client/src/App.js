// remove line breaks & whitespaces
// let htmlEdit = htmlText.replace(/(\r\n|\n|\r)/gm, "").replace(/\s/g, "");

function App() {
  return (
    <div>
      <h1>Hello</h1>
    </div>
  );
}

export default App;
